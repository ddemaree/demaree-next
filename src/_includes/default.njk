<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<title>{{ title }}</title>
		<link href="/css/main.css" rel="stylesheet" type="text/css" />
	</head>
	<body class="dark-mode{% if slug %} page--{{slug}}{% endif %}">
		<div class="dd-container">
			{% include 'header.njk' %}

			<main class="dd-main py-8 {% if main_classes %} {{ main_classes }}{% endif %}">
				<div class="dd-wrap">
				{{ content | safe }}
				</div>
			</main>

			{% include "footer.njk" %}
		</div>
		<script src="/bundle.js"></script>
	</body>
</html>
