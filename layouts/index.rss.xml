{{- $pages := where site.RegularPages "Type" "in" site.Params.mainSections -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ .Site.Title }}</title>
    <link>{{ .Permalink }}</link>
    <description>All articles &amp; notes from demaree.space</description>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Author.email }}
    <managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Author.email }}
    <webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{ range $pages }}
    <item>
      <title>{{ .Title }}{{ if .Params.link_url }} &rarr;{{ end }}</title>
      {{- if .Params.link_url -}}
      <link>{{ .Params.link_url }}</link>
      {{- else -}}
      <link>{{ .Permalink }}</link>
      {{- end -}}
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}
      <guid>{{ .Permalink }}</guid>
      <description type="html">{{ print `<![CDATA[ ` | safeHTML }}
      {{ .Content }}
      {{- if .Params.link_url -}}
      <p><a href="{{ .Params.link_url }}"><b>External Link</b></a> | <a href="{{ .Permalink }}"><b>Link to my post</b></a></p>
      {{- end -}}
      {{ print `]]>` | safeHTML }}
      </description>
    </item>
    {{ end }}
  </channel>
</rss>